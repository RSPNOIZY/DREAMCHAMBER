# Requirements for Bobby Icon Ritualize

# No external Python packages required
# This utility uses only Python standard library modules:
# - os (built-in)
# - subprocess (built-in) 
# - pathlib (built-in)
# - shutil (built-in)

# External dependencies (installed via system package manager):
# - fileicon (install via: brew install fileicon)