version: "3.9"
services:
  stt:
    image: fedirz/faster-whisper-server:latest-cpu
    container_name: noizy-stt
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./ops/hf_cache:/root/.cache/huggingface
    restart: unless-stopped
